{
  "name": "rsbuild-react-js",
  "homepage": "http://kathrynrichardson.github.io/tic-tac-toe-game",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.1.4",
    "@rsbuild/plugin-react": "^0.1.4",
    "gh-pages": "^6.1.0"
  }
}
